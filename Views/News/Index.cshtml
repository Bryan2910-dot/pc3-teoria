@model List<pc3_teoria.Models.PostWithExtrasViewModel>

<h2>Noticias</h2>

<ul>
@foreach (var post in Model) {
    <li>
        <a href="@Url.Action("Details", "News", new { id = post.Id })">@post.Title</a>
    </li>
}
</ul>